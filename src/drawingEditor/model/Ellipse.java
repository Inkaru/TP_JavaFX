package drawingEditor.model;

import javafx.scene.paint.Paint;

public class Ellipse extends FormeImpl {
    public Ellipse(double x, double y, double hauteur, double largeur, Paint col) {
        super(x, y, hauteur, largeur, col);
    }
}
