package drawingEditor.model;

import javafx.scene.paint.Paint;

public class Rectangle extends FormeImpl {

    public Rectangle(double x, double y, double hauteur, double largeur, Paint col) {
        super(x, y, hauteur, largeur, col);
    }

}
